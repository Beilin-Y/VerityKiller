name: Build Mod Jar
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v5
        with:
          java-version: 17
          distribution: temurin
      - uses: gradle/actions/setup-gradle@v4
      - name: Build with Gradle (wrapper if present)
        run: |
          set -e
          # Prefer wrapper at repo root
          if [ -f "./gradlew" ]; then
            chmod +x ./gradlew
            ./gradlew build --no-daemon
          else
            # Search for gradlew in the repository (first match)
            GW=$(find . -type f -name gradlew -print -quit || true)
            if [ -n "$GW" ]; then
              DIR=$(dirname "$GW")
              chmod +x "$GW"
              (cd "$DIR" && ./gradlew build --no-daemon)
            else
              echo "No gradlew found in repo — falling back to system 'gradle'"
              gradle build --no-daemon
            fi
          fi
      - uses: actions/upload-artifact@v4
        with:
          name: VerityKiller-Mod
          path: build/libs/*.jar
